# リリース前の手動確認

`npm test` が全件（208件）通っていることを確認してから行う。
各項目は**新しいプレイヤーを作った状態**から始める。

## 1. はじめる
- [ ] 初回起動でいきなり作成画面が出る
- [ ] 名前が空、またはキャラ未選択では「はじめる」が押せない
- [ ] 名前に `<b>あ</b>` と入れても太字にならない
- [ ] 作成後ホームに移り、Lv1・EXPバーが空で表示される

## 2. きろく
- [ ] ノーバウンド 10 を記録 → +30 EXP、「じこベスト こうしん！」、レベルアップ
- [ ] 同じ日に 5 を記録 → +0 EXP と案内が出る
- [ ] 同じ日に 12 を記録 → +6 EXP（差分 2 × 3）
- [ ] ワンバウンド 50 を記録 → +50 EXP
- [ ] 0 のままでは記録できない。5桁目は入力できない
- [ ] 「けす」で桁が減る

## 3. レベルと進化
- [ ] ホームのEXPバーとレベルが更新される
- [ ] 「しんかの じょうけん」の達成項目に ✅ が付く
- [ ] レベル条件だけ満たすとキャラが光り「なにかが おこりそう…！」が出る
- [ ] 3条件すべて満たすと結果画面で進化演出が流れる
- [ ] 同じ進化が二度演出されない

## 4. なかま
- [ ] Lv10 で「あたらしい なかまが まってるよ！」が出る
- [ ] 御三家の残り2体から1体を選べる
- [ ] 受け取っただけでは育成キャラが変わらない
- [ ] 切り替えると新しいキャラは Lv1、元のキャラに戻すとレベルが残っている
- [ ] ニックネームを付けるとホームにも反映される

## 5. ずかん
- [ ] 未所持はシルエットと「？？？」
- [ ] 未所持の詳細では説明文と進化条件が見えない
- [ ] 所持の詳細に説明文・タイプ・特性・進化系統図・進化条件が出る
- [ ] 第1進化に「どちらかで OK」、第2進化に「ノーバウンド だけ」と出る

## 6. きろく帳
- [ ] 記録日にドット、自己ベスト更新日に★が付く
- [ ] 月を前後に移動できる
- [ ] 日をタップするとその日の記録が**端末のローカル時刻**で出る（UTCのまま
      ずれて表示されていないか確認。日本時間の夜の記録が翌日や昼に見えたら不具合）
- [ ] グラフに2系列が出る。30／90／ぜんぶを切り替えても崩れない
- [ ] **記録の間隔を7日以上あけて2件記録すると、グラフの線がその間で途切れる**
      （7日未満の間隔ではつながる）。境界値の調整は `js/svg/chart.js` の
      `GAP_BREAK_DAYS`

## 7. 親の承認
- [ ] ON にするとパスワード2回とあいことばを聞かれる
- [ ] localStorage にパスワードが平文で入っていない
- [ ] 記録が承認待ちに入り EXP が増えない
- [ ] 承認待ちがカレンダー・グラフ・自己ベストに出ない
- [ ] パスワードを間違えると一覧が見えない
- [ ] 承認すると EXP が入り、ホームに「みとめてくれた！」が1回だけ出る
- [ ] 回数を直して承認すると直した値が記録される
- [ ] 「けす」を選ぶと記録が残らず EXP も増えない
- [ ] 承認待ちの時刻表示も**ローカル時刻**になっている
- [ ] ホームに戻って承認画面を開き直すと再びパスワードを聞かれる（一度開いた
      認証状態はこの画面を離れると保持されない）
- [ ] パスワードを空欄にするとあいことばで解除できる

## 8. バックアップ
- [ ] 書き出すと `liftingmaster-backup-YYYY-MM-DD.json` がダウンロードされる
- [ ] 記録を足してから読み込むと足す前に戻る
- [ ] 壊れた JSON を読み込むとエラーが出て今のデータが消えない（設定画面の
      読み込み機能）
- [ ] **起動時に localStorage の中身自体が壊れている場合**、初期状態
      （新規作成画面）で起動し、壊れていた元データは消えずに退避される
      （`liftingmaster.v1.broken` キー）。ブラウザの開発者ツールで
      Application → Local Storage を開き、既存の値を壊れた文字列に書き換えて
      から再読み込みして確認する
- [ ] さらにもう一度壊れたデータで起動すると、2件目は `liftingmaster.v1.broken.2`
      に退避される（1件目の `.broken` は上書きされず残る。3件目以降は
      `.broken.2` が上書きされる仕様）

### Task 27: バックアップを端末の外へ出す・節目で促す
- [ ] 書き出しで共有シートが出ること（対応端末。iPad の Safari など）／
      PC のブラウザ（共有シート非対応）では従来どおりダウンロードになること
- [ ] 共有シートを取り消しても（「キャンセル」を選んでも）エラーが出ず、
      せっていの「まえに ほぞんしたのは」も変わらないこと
- [ ] 共有・ダウンロードのどちらでも、成功後にせっていの
      「まえに ほぞんしたのは ◯がつ◯にち」が更新されること
- [ ] 進化した結果画面の最後に「おうちのひとへ: きろくを ほぞんして
      おくと あんしんです」の案内が出ること。14日以内に再度進化しても
      出ないこと（新しい仲間・レベル10またぎでも同様）
- [ ] 古いバックアップ（`lastBackupAt` の無い JSON）を「よみこむ」で
      読み込んでも壊れないこと（後方互換）

## 9. PWA
- [ ] Service Worker が activated になる
- [ ] サーバーを止めても再読み込みで起動する
- [ ] ホーム画面に追加すると全画面で起動する
- [ ] アイコンが青地に白いボールで表示される

## 10. 端末での確認
- [ ] タブレット（実機）で全ボタンが押しやすい大きさである
- [ ] 縦向きで横スクロールが発生しない
- [ ] 数字入力でソフトキーボードが出ない

## 11. 過去の日付での記録・ベスト表示（Task 26）
- [ ] きろく画面の上部に対象日が出る。既定は「きょう」ボタンが選択状態
- [ ] 「きのう」を選んで記録できる。記録後の結果画面の見出しに日付が出て
      「きょう」とは言わない
- [ ] カレンダー（`<input type="date">`）から3日前を選んで記録でき、
      きろく帳のカレンダーにその日の印（ドットまたは★）が付く。その日を
      タップした詳細にも記録が出る
- [ ] 未来の日付は選べない。カレンダーの日付ピッカーで未来日を選ぼうとしても
      弾かれ、理由（「みらいの ひは えらべないよ」）が出て記録ボタンが
      押せなくなる
- [ ] ちょうど1年前は選べるが、それより前は選べない（理由「1ねんより
      まえは えらべないよ」が出て記録ボタンが押せない）
- [ ] 過去日を選んでいる間は、日付が目立つ形（バナー）で表示され続ける。
      「きょう」ボタンを押すとバナーが消える
- [ ] 記録が1件もない日・モードを選ぶと「まだ きろくが ないよ」と出る。
      記録がある日・モードでは「（きょう／◯がつ◯にちの） ベストは
      ◯かい」と出て、数字はその日・そのモードの最高回数と一致する
- [ ] ノーバウンドとワンバウンドを切り替えると、ベスト表示がその場で
      切り替わる（別々の記録として扱われる）
- [ ] 2回目の記録で1回目より少ない回数を入れると +0 EXP になり、結果画面に
      「（きょう／◯がつ◯にちの） ベスト ◯かいを こえると EXPが
      もらえるよ」と、超えるべき具体的な回数が出る
- [ ] 日をあけて記録した状態（例: 3日前と1日前だけ記録し、2日前は未記録）で
      ホームの「れんぞく きろく」が途切れていることを確認したあと、
      2日前をカレンダーから選んで記録すると、「れんぞく きろく」が
      つながって日数が伸びる（コア層の集計をそのまま使うだけで実現できる
      ことの確認）
- [ ] 親の承認をONにした状態で過去日を記録すると、今日の記録と同じように
      承認待ちに入り、結果画面は「しょうにん まち」の案内になる。この
      ときも見出しは選んだ日付が基準で「きょう」とは言わない

---

### Task 28: ひのこを画像で描く（残り8体はSVGのまま）
- [ ] ホーム・なかま・ずかん・結果演出・進化演出のいずれでも、ひのこが画像
      （SVGの図形ではなく用意されたイラスト）で表示されること
- [ ] ずかんで未所持のひのこが、色のない灰色のシルエットとして表示されること
      （「？？？」表示とあわせて、輪郭からそれとわかってしまわないか確認）
- [ ] ひのこ以外の8体は、これまでどおりSVG（コード生成の図形）で表示され、
      見た目が変わっていないこと
- [ ] 機内モードにしても、ひのこの画像が（灰色の四角やアイコン切れになど
      ならず）これまでどおり表示されること（sw.js のキャッシュ確認）

## 12. この環境では確認できていないこと（要・実機確認）

このリポジトリの開発環境にはブラウザを描画してスクリーンショットを撮る手段が
なく、10画面・9キャラクター・進化演出のいずれも、実装した本人以外はまだ
一度も目で見て確認していない。テストは計算結果（EXPの数値、進化条件の判定
など）が正しいことしか保証しないため、以下は**別途、実機・実ブラウザでの
確認が必要な残作業**として扱うこと。

- [ ] キャラクター9体・27パターン（3進化段階）の見た目が、子供にとって
      魅力的か。左右非対称・配色のバランスなど、SVGの座標だけを見て判断した
      造形が実際にどう見えるか
- [ ] 進化演出（光る→結果画面での進化アニメーション）が「わくわくする」体験に
      なっているか。テストは「演出が1回だけ流れる」ことしか検証しておらず、
      間の取り方・派手さは未評価
- [ ] タブレット実機でのレイアウト崩れ（本チェックリスト10章と重複するが、
      特に横向き回転時や、画面サイズが異なる複数機種での確認は未実施）
- [ ] ずかんの進化系統図・きろく帳のグラフなど、SVGを直接生成している画面が
      実ブラウザで意図通りにレンダリングされるか（コード上のviewBox計算は
      テスト済みだが、目視は未実施）
- [ ] 長期間（数ヶ月〜Lv100到達）使い続けたときの体感バランス。数値上の
      カーブはテストしてあるが、遊んでいて「ちょうどよい」かは未検証
